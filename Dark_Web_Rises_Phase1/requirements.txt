--extra-index-url https://download.pytorch.org/whl/cpu

fastapi==0.136.1
uvicorn[standard]==0.46.0
httpx==0.28.1
# Pinned <2.0: torch==2.3.1's compiled extensions are not ABI-compatible
# with numpy 2.x. An unpinned `numpy` resolves to the latest 2.x release on
# a fresh install and torch fails at runtime with "RuntimeError: Numpy is
# not available" the first time it touches a numpy array (e.g. inside
# compare_image's image preprocessing) instead of failing at import time.
numpy>=1.24,<2.0
pillow==12.2.0
nltk==3.9.4
python-dotenv==1.2.2
torch==2.3.1+cpu
torchvision==0.18.1+cpu
open_clip_torch
openxyl
# Used only by reader.py, which is not currently imported by the running
# app (see README "Future: database migration"). Kept for the planned
# database-migration phase; safe to remove if reader.py is deleted first.
supabase

# --- test/dev only ---
pytest==8.3.3
pytest-asyncio==0.24.0